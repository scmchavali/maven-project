keytool -genkey -keyalg RSA -alias selfsigned -keystore serverStore.jks -storepass
password -validity 360 -keysize 2048

To call secured server from client, we should configure HTTPS request configuration trust store as below

In Trust Store Configuration, add the following:
• Path: serverStore.jks
• Password: password
• Type: JKS